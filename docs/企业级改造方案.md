# 企业级 Agent 项目改造方案

> 本文档只记录改造的目标、范围、模块设计与实施步骤等关键信息，作为施工蓝图。
>
> 不记录具体代码改动方式、不记录零碎小问题。代码层面的实现细节另行讨论。

## 一、目标与原则

- **目标**：把当前偏 demo 的单 Agent RAG，升级为「小而美、五脏俱全」的企业级 Agent 项目，最终仍发布到 ModelScope 单容器。
- **定位升级（2026-09）**：牛来不再自我设限为「企业知识库问答助手」，目标是做成**能力齐全、非常能打的企业级 Agent**——知识库 RAG + 联网搜索 + 工具编排 + 长期记忆 + 可观测性 + 离线评估一个不落。凡能显著增强 Agent 真实能力、且不产生持续费用（$0）、不违背 ModelScope 单容器约束的功能，都纳入范围（首个即步骤 8 联网搜索）。
- **原则**：
  - 功能齐全但不堆砌，每加一项都要能讲清「为什么值得」。
  - 依赖尽量不膨胀，优先复用已有能力与标准库。
  - 一切设计以 ModelScope 约束为前提：单容器、CPU 推理、无外部服务、出网受限。
  - 生产路径只保留一种编排实现（多 Agent 图），旧的对照实现在改造中清理。
  - **韧性兜底（分层降级、失败留痕、永不裸奔）**：每个可能失败的环节都要有明确降级路径，失败时给用户体面回应而非崩溃/空白，同时记一笔日志/trace 便于排查。只做必要兜底、不做过度兜底——重试只在模型调用收口层做（不层层重试），降级不得静默吞异常；「模型答错」靠事前预防与离线评估解决，不做在线自动纠错（详见决策 6）。

## 二、范围边界

**做：**

- RAG 质量三件套（结构化切片 + 混合检索 + Reranker）与 Self-CRAG 质检
- 多 Agent 编排（真实职责拆分，唯一生产路径）
- 后端 JWT 真鉴权 + 游客/亮哥双身份 + 私人内容隔离
- SQLite 持久化（长期记忆 + 全链路 trace）+ tenacity 韧性
- 可观测性第一档（token/成本/延迟）+ 第二档（自建轻量 trace）
- Agent 长期记忆（跨会话，轻量方案 + 治理）
- 代码清理（删对照实现）+ 依赖精简
- RAG 质量保障（离线评估脚本 + 在线质量监控面板）
- Prompt 注入防御（输入与检索内容护栏，**必做**）
- 联网搜索能力（接入 $0 免费搜索源，让 Agent 应对开放式实时查询：新闻/股价/事实核查等，形成「搜索 → 抓取 → 作答」闭环）

**不做：**

- 第三档可观测性（LangSmith / OpenTelemetry / Langfuse 等外部平台）
- 多租户（只保留游客、亮哥两个身份）
- ORM、Postgres、Redis 等重型工程化组件
- 把 RAG 评估做成常驻服务（离线评估始终是手动跑的脚本；步骤12B 的在线面板只读已落库的 trace 做聚合展示，不是常驻评估服务，不违背本条）
- 记忆系统的「每轮抽取」与「框架 Store」重型方案

## 三、已确认的关键决策

1. **Reranker 模型**：`bge-reranker-base` 的 ONNX int8（容器内存宽裕时可升级 `bge-reranker-v2-m3`）。模型文件需手动放入模型资产目录；代码在模型缺失时优雅降级为纯 RRF 排序，不影响系统运行。
2. **新增依赖**：仅 `jieba`（中文分词）与 `rank_bm25`（关键词打分）。其余能力（JWT、重试、ONNX 推理、编排）均已在现有依赖中。
3. **数据持久化**：SQLite，使用 Python 标准库 `sqlite3`，不引入 ORM。
4. **编排实现**：删除手写版与 LangChain 版对照代码，多 Agent 图为唯一生产路径。
   - 注意：删的是「对照代码」，不是 langchain 相关「库」；多 Agent 图仍依赖 LangChain 生态（模型客户端、工具封装、MCP 适配）。
   - 共享零件（知识库工具封装、技能加载、模型客户端）保留复用。
5. **知识库测试/演示语料方向**：采用虚构「牛来数码」公司设定（对内答公司制度、对外答产品）。知识库含公司制度 + 产品目录（型号/参数/价格/保修）+ 售后 FAQ，刻意埋入“相似但不同”的区分度陷阱（如 N7 / N7 Pro / N7s 参数各异），用于验证混合检索与精排的价值。语料须原创虚构（不用真实淘宝内容，规避版权与营销噪音）、事实密集、可标注金标准。牛来的人格/规矩等写死提示词可随时修改，语料主题不受其现有定位约束。
6. **「模型答错」的处理**：不做在线自动纠错（再叫一个模型核查会同样出错、且徒增延迟与成本）；改为事前预防——强制 grounding（照资料答 + 编号引用）、人格允许「没把握就说不知道、禁止编造」、护栏（越界/有害/泄露内部信息一律拒答；**防 prompt 注入**——用户输入或检索内容里试图改写指令、套取系统提示词、诱导越权时一律不执行，只按既定人格与规则作答）、需结构化输出时解析失败则重试或降级。答案对错交给步骤 12 离线评估量化，据此改 prompt / 检索 / 语料，而非在线兜底。
7. **联网搜索源（$0，2026-09 核实）**：优先 **Tavily 免费档**——每月 1000 credits（basic 搜索 1 credit/次 ≈ 1000 次/月）、注册免信用卡、agent 原生返回干净正文、LangChain 有内置集成、走正规 API 故数据中心 IP（ModelScope 容器）稳定，只需一个免费 key 进环境变量；免费档不绑卡、超额才付费，demo/面试用量远够。备选 **DuckDuckGo `ddgs`**（纯 pip、零 key，但非官方、易被风控限流，数据中心 IP 尤甚，更适合本地）。两者均 **$0**。（Brave 自 2026-02 取消纯免费档改 $5/月额度、Bing Search API 已退役，均不作首选。）

## 四、模块设计（能力层面，不含实现细节）

### 模块 1 · RAG 质量层

- **结构化切片**：替代当前「压平换行 + 定长硬切」，改为按标题/段落分节，切片携带结构元数据。
- **混合检索**：向量召回 + 关键词（分词 + BM25）召回，用 RRF（倒数排名融合）合并，不引入加权调参。
- **Reranker 精排**：召回阶段宽松取较多候选，精排阶段收窄到少量高质量结果，彻底替代旧的距离硬阈值。
- **Self-CRAG 质检**：精排后复用 Reranker 分数当「质检员」逐片判定相关性、过滤低相关片；全不相关则如实告知「无资料」。「有界重写重查一次」放到检索质检 worker（模块 2）里做——靠 Supervisor 意图路由只对真·知识库查询触发，避免拖累闲聊。
- **检索兜底**：召回为空或资料不相关由 Self-CRAG 处理（如实告知 / 有界重写重查一次）；Reranker 模型缺失降级为纯 RRF；检索层任何异常（向量库/BM25 索引报错、超时）都须 catch 并降级为「无上下文谨慎回答或致歉」，绝不把堆栈抛给用户。

### 模块 2 · 多 Agent 编排（唯一生产路径）

- 基于 LangGraph 手写 StateGraph，共享状态贯穿各节点。
- **Supervisor**：意图识别、复合问题任务拆分（query decomposition）与路由。
- **检索质检 worker**：改写 → 混合召回 → 精排 → 质检（承接模块 1 能力）；质检判定不相关时，有界地重写查询重查一次（仅对真·知识库查询触发，闲聊不走此路）。
- **工具 worker**：集中管理外部工具（网页抓取、天气、**联网搜索**），主 Agent 不再平铺全部工具；「搜索 → 抓取 → 作答」形成实时信息闭环（应对股价/新闻等开放式实时查询）。Supervisor 的 `tool` 意图与工具说明同步纳入「联网搜索实时信息」。
- **合成 worker**：基于质检过的资料生成回答，流式输出 + 编号引用；施加 prompt 注入护栏（把资料当数据不当指令、拒绝改写人格/套取系统提示词/诱导越权）。
- 拆分原则：每个 worker 有独立职责，避免「为多 Agent 而多 Agent」。
- **编排韧性（防「互相甩锅」）**：设 `recursion_limit` 步数上限并捕获 `GraphRecursionError`，超限走兜底合成或致歉，杜绝死循环/踢皮球；Supervisor 设默认路由（catch-all），某 worker 失败或返回空则跳过它、带已有信息继续；每个状态单一 owner，无 worker 认领时回落默认路径。
- **复合问题任务拆分（步骤10）**：Supervisor 把复合问题拆成原子子任务，用 LangGraph `Send` 按子任务数动态派发 N 个 worker 并行处理（kb 子查询各自检索、tool 子查询走工具），子结果归并后统一连续编号、分点合成——根治「整句检索」的主题互相稀释（详见步骤10）。

### 模块 3 · 鉴权与私人内容隔离

- **双身份**：游客（无 token）、亮哥（登录持 token）。身份决定「能看 / 能用哪些内容」。
- **鉴权**：口令（bcrypt 哈希）与密钥仅存后端环境变量，绝不进前端包；登录签发长效 token（默认 30 天），前端保存 token 并在请求头携带。
- **上传归属**：游客可上传，但一律公共（匿名无归属，忽略私人标记）；亮哥上传可自选公共或私人。
- **私人隔离**：检索、清单、下载对游客一律过滤私人内容；私人内容仅亮哥可见可用。
- **删除权限**：删除仅亮哥（游客一律 403），且仍受禁删名单约束；前端删除按钮仅亮哥渲染。
- **退出登录**：JWT 无状态，退出 = 前端清掉本地 token（不做服务端黑名单，靠 30 天过期兜底，已知取舍）。
- **前端呈现**：文件带 `private` 字段，卡片显示「公 / 私」徽章。
- **纵深防御**：前端隐藏按钮 = 体验（界面干净），后端每次请求重新验 token + 403 = 安全（前端可被绕过，真正安全线在后端）；两者独立、缺一不可。

**权限矩阵：**

| 接口                           | 游客              | 亮哥             |
| ---------------------------- | --------------- | -------------- |
| `/api/chat` 检索               | ✓ 仅公共           | ✓ 公共 + 私人      |
| `/api/files` 清单              | ✓ 仅公共（私人不可见）    | ✓ 全部 + 公 / 私徽章 |
| `/api/upload` 上传             | ✓ 只能传公共         | ✓ 公共或私人        |
| `/api/files/{n}` 删除          | ✗ 403 + 前端不显示按钮 | ✓（仍受禁删名单约束）    |
| `/api/files/{n}/download` 下载 | 公共 ✓ / 私人 ✗ 403 | ✓ 全部           |
| `/api/login` 登录              | ✓               | —              |
| 退出登录                         | —               | ✓（前端清 token）   |

### 模块 4 · 韧性 + 可观测性

- **韧性**：所有模型调用统一收口，包重试、超时、降级。
- **可观测性第一档**：记录每次模型调用的 token、成本、延迟。
- **可观测性第二档**：把一次问答的全链路（改写→召回→精排→质检→合成，各步耗时与 token）记为一条结构化 trace 存 SQLite；可选极简查看入口。
- 说明：韧性与两档可观测性都在模型调用收口处一并实现，避免逻辑散落；各层兜底/降级触发都要记进 trace（降级留痕），不允许静默吞异常。

### 模块 5 · RAG 质量保障（离线评估 + 在线监控）

分两轨，数据源与目的不同、不可互相替代：离线像「单元测试」（有金标准断言、测**答得对不对**），在线像「APM/Sentry」（看真实流量、测**系统转得顺不顺**）。

- **5A 离线评估（开发期，不进运行时）**：由 LLM 读知识库文档自动生成问答对（问题 + 参考答案 + 参考来源，人工快速抽检修正）；检索层出 Recall@k / MRR / NDCG，生成层用 LLM-as-judge 打忠实度、答案相关性；并用评估集标定 Self-CRAG 相关性阈值（还掉 `rag.py` 里「0.0 是原理性默认值」的债）；产出报告文件。
- **5B 在线质量监控面板（运行时，亮哥专属）**：复用步骤9 已落库的 trace（无需新增埋点），聚合出漏标率（citation_miss）、平均成本、降级率等真实流量指标，前端可视化展示；即激活步骤9「后续可选增强·运行看板」并聚焦到 RAG 质量维度。
- 定位：5A 是开发期离线工具、独立脚本、不打包进 ModelScope 运行时；5B 是上线后的质量仪表盘。两者互补——5B 无金标准、测不了「答得对不对」，替代不了 5A。

### 模块 6 · Agent 长期记忆（跨会话）

- **定位**：亮哥专属；游客不记不读。区别于短期记忆（会话历史，已由模块 4 覆盖）。
- **判断该记什么**：由 LLM「记忆官」按评判清单抽取——记身份/画像、稳定偏好、长期事实、明确指令、重要决定及理由；不记一次性任务细节、闲聊、本轮中间过程、瞬时状态。总闸：「跨会话是否仍有价值且稳定」。
- **隐私红线**：密码、令牌、身份证、银行卡等敏感信息，即使被要求也绝不记录。
- **时机（轻量方案）**：会话结束时异步抽取，主链路零延迟；抽取看完整段对话，判断更干净。
- **触发策略**：新开对话时惰性补抽上一段 / 每积累 N 轮增量抽取 / 闲置补做 / 强信号（「记住…」「我喜欢…」「以后都…」）即时捕获。
- **去重与冲突**：入库前与已有记忆比对，决定新增/更新/删除旧的/跳过，防止记忆自相矛盾。
- **存储与注入**：记忆条目带类别与关键词存储；提问时复用混合检索捞出相关记忆，注入合成提示词。
- **治理入口**：提供「我的记忆」查看/删除，作为记错时的纠错托底。
- **原则**：宁可不记，不要记错。

### 模块 7 · 代码与依赖清理

- **代码清理**（在多 Agent 图成为生产路径后进行）：删除手写版与 LangChain 版对照代码、旧的单 Agent 图、相关旧开关；保留共享零件与 langchain 库。
- **依赖精简**（压轴进行）：以「重建干净虚拟环境 + 只声明直接依赖 + 冻结生成精确依赖清单」的方式收敛 `requirements.txt`，避免肉眼删行误删间接依赖。
- **死文件清理**：候选文件逐个确认后再删，不擅自清理。

### 模块 8 · 部署适配（ModelScope）

- 构建镜像安装新依赖、打包 Reranker 模型资产；核验 ONNX 未变成 LFS 指针文件。
- **知识库语料与 seed 机制**：保留 `seed.py`「每次冷启动灌库」机制——因 `uploads/`、`chroma_db/` 被 gitignore 且创空间容器临时，手动上传会在重启后丢失，故线上公共底料必须靠 seed 预置，**绝不删 seed**；将种子文档由硬编码字符串改为读取随仓库提交的 `backend/seed_docs/*.txt`（内容见决策 5 的「牛来数码」语料），Dockerfile 增加对应 COPY。`upload` 接口保留，定位为运行时临时加料与私人文档（模块 3），不作为线上底料来源。私人示例文档不预置。
- CPU 上多 Agent 会放大延迟，worker 数量与重试次数需克制。

## 五、实施步骤与提交节点（打勾清单）

> 规矩：一步 = 完成一个功能 = 做一次代码提交。每完成并验证一步，就手动把对应复选框打勾（`[ ]` 改成 `[x]`），并提交一次代码。
>
> 复选框由你本人手动勾选，助手不代劳（老规矩）。
>
> 每步应可独立验证；改造期间若启动本地服务，收尾前须彻底停止（进程列表 + 端口监听两项均确认为空）。
>
> 提交信息仅为建议，可按习惯调整。

- [x] **步骤 1 · 依赖与 Reranker 资产**：加入 `jieba` / `rank_bm25`，放入 Reranker ONNX 模型资产，单独验证打分能力（模型缺失时能降级）。
  - 提交点：`feat: 引入 reranker 模型资产与检索新依赖`
- [x] **步骤 2 · 结构化切片 + 混合检索 + 精排**：切片改造、向量 + BM25 + RRF 融合、Reranker 精排替代旧距离硬阈值，离线验证检索质量；**检索层异常统一 catch 并降级**（向量库/BM25 报错或超时不把堆栈抛给用户，退化为无上下文谨慎回答）。
  - 提交点：`feat: RAG 结构化切片与混合检索精排`
- [x] **步骤 3 · Self-CRAG 质检**：精排后相关性判定（复用 Reranker 分数当质检员）+ 逐片过滤低相关片 + 无资料时如实告知；**有界重写重查挪到步骤 5 的检索质检 worker**（靠意图路由只对真·知识库查询触发，不拖累闲聊）。
  - 提交点：`feat: 检索结果 Self-CRAG 质检`
- [x] **步骤 4 · 鉴权与私人隔离**：JWT 登录 + 游客/亮哥双身份；上传归属（游客只能传公共、亮哥可传公共或私人）；检索/清单/下载对游客过滤私人；删除仅亮哥（游客 403 + 前端不显示按钮）；前端公/私徽章 + 退出登录（清本地 token）；纵深防御（前端隐藏 = 体验、后端 403 = 安全）。
  - 提交点：`feat: JWT 鉴权与私人内容隔离`
- [x] **步骤 5 · 多 Agent 图成为生产路径**：StateGraph（Supervisor + 检索质检/工具/合成 worker）接入对话接口，保持流式与引用卡片；**检索质检 worker 内落地 Self-CRAG 的「有界重写重查一次」**（只对真·知识库查询触发，闲聊不走此路）；**同步落地 Prompt 注入防御**（人格/系统提示词硬化、把检索内容当数据而非指令、拒绝套取系统提示词与越权诱导）；**编排韧性（防「互相甩锅」）**——设 `recursion_limit` 步数上限并捕获 `GraphRecursionError`、Supervisor 配 catch-all 默认路由、某 worker 失败或返回空则跳过它带已有信息续跑，杜绝死循环/踢皮球。
  - 提交点：`feat: 多 Agent 编排接入生产路径`
- [x] **步骤 6 · 代码清理**：删除手写版 / LangChain 版对照代码与旧单 Agent 图、旧开关（保留共享零件与 langchain 库）；**死文件清理**（候选文件逐个确认后再删，不擅自清理）。
  - 提交点：`refactor: 移除手写版与 LangChain 版对照实现`

> **原步骤7 · 会话持久化：已砍**（2026-09 决策）。理由：单独做属 CRUD 管道活、AI-Agent 谈资弱；其「后端存储层」能力并入步骤11 长期记忆（记忆本就必须持久化，名头也更值钱）。前端会话继续用 localStorage、后端维持无状态。**步骤由 14 收敛为 13。**

- [x] **步骤 7 · 知识库语料替换与 seed 改造**：按「牛来数码」方向（见决策 5）编写虚构测试语料（公司制度 + 对外产品目录 + 售后 FAQ，型号/参数/价格埋区分度陷阱），替换现有演示文档；`seed.py` 由硬编码字符串改为读取随仓库提交的 `backend/seed_docs/*.txt`，保留“每次冷启动灌库”机制（绝不删 seed），Dockerfile 增加对应 COPY。（提前理由：给步骤12 评估埋区分度陷阱、并刷新 demo 底料）
  - 提交点：`feat: 替换知识库演示语料并改造 seed 注入`
  - **补充发现问题（2026-09）· 作用域歧义 + Supervisor 能力清单**：年假类“光杆政策问题”暴露作用域歧义（问公司制度还是全国标准）。链路病症：Supervisor 盲判（不知库里有啥）→ 改写把“年假”漂成“职工带薪年休假”（法规术语）→ Self-CRAG 判无资料 → 合成拿通用常识硬答背了《条例》。**方案**：① 给 Supervisor 喂**自动生成的能力清单**（KB 范围=切片元数据聚合 filename+section、按身份过滤；工具清单=get_mcp_tools/skill 的 name+描述），替掉写死的工具白话；② Supervisor 新增正交 **scope**（company/general/both）+ few-shot；③ 改写受 scope 约束（company/both 保留公司口径、禁漂法规术语）；④ 合成按 scope 分流（both=公司带溯源卡片 + 通用自然措辞，不贴硬标签）。**附**：小节标题优化（保修政策→各型号保修期限、保修范围条款→保修范围与免责情形、考勤与休假→点出年假等子主题、三款怎么选→三款型号选购对比），标题只进 metadata 不进正文、**纯改清单标签不影响检索**。通用那半的“实时+出处”待步骤8 联网搜索接入。
- [ ] **步骤 8 · 联网搜索工具（Web Search）** ⏸️【2026-09-19 暂缓·非当前刚需】：给工具 worker 接入 $0 免费搜索源（见决策 7：Tavily 免费档优先 / DuckDuckGo 备选），让牛来能应对开放式实时查询（股价/新闻/事实核查）。链路：`search_web(query)` → 候选 URL + 摘要 →（需要时）`fetch(url)` 取正文 → 合成时**如实标注网络来源**（区别于知识库 `[n]` 卡片，展示来源 URL/站点）。同步更新 Supervisor 意图提示词（`tool` 纳入「联网搜索」）与工具说明；搜索失败/被限流按韧性原则降级留痕、如实告知，绝不编造数字。
  - 提交点：`feat: 接入联网搜索工具`
  - 备注：已按用户要求提到部署之前（原排在部署后作再发布内容）；补上「苹果市值」那类开放式实时查询的洞，$0、自包含、不依赖持久化。
  - ⏸️ **暂缓说明（2026-09-19）**：用户评估联网搜索非当前刚需，优先级下调、暂时搁置，本步暂不实施（复选框保持未勾，非「已完成」）。接入形式与选型调研全部保留备用——本地 `@tool` + Tavily SDK 优先；不用 DeepSeek 内置 `web_search`（黑盒、拿不到结构化来源做不了溯源、架空 Supervisor 意图路由、锁定单一模型；详见决策 7）。未来若重启，直接照此实施。**实施顺序改为 7 → 9。**
- [x] **步骤 9 · 模型调用收口**：统一封装重试/超时/降级 + 可观测性第一档（token/成本/延迟）+ 第二档全链路 trace；**降级留痕**（各层兜底/降级触发均记进 trace，不允许静默吞异常）。
  - 提交点：`feat: 韧性封装与轻量可观测性`
  - **落地现状（2026-09-19）**：双轨埋点（裸 client 走 `llm_gateway.chat()` 手动埋、LangChain 走 `TokenMeter` 回调自动埋，可捕获 ReAct 子 agent 内部每次调用）+ contextvars 请求级归集 + 全链路 trace 落 SQLite（`backend/app/db.py` 的 `traces` 表，当前只写）；前端回答角落 meta（全员）+ 🔍 推理链路抽屉（仅亮哥，走 SSE 实时透出、不查库）。第一档、第二档均已实现。
  - ⏸️ **后续可选增强（2026-09-19 决策：选做但暂缓）· 运行看板**：当前 `traces` 表**只写不读**——历史 trace 已落库，但没有读路径（模块4 的「可选极简查看入口」目前只覆盖单次问答的 SSE 实时展示，未做历史聚合）。后续可补一个**极简运行看板**：后端加 `GET /api/traces?limit=N`（最近 N 条）+ `GET /api/stats`（聚合平均成本 / 降级率 / 各 purpose 的 token 分布），前端加亮哥专属「📊 运行看板」页，把历史 trace 激活成可视化。价值：补齐可观测性闭环（埋点→存储→可视化）+ 强面试谈资。**非刚需故暂缓，重启时直接照此实施。**（2026-09-20 更新：此运行看板已并入**步骤 12B「在线质量监控面板」**&#x4E00;并实施，聚焦 RAG 质量维度。）
- [x] **步骤 10 · 意图识别任务拆分（复合问题分解 + 动态并行编排）**：把 Supervisor 从「多标签意图分类」升级为「原子子任务拆分器」——复合问题（如「明天上海天气 + 公司年假 + 餐补」）拆成多个独立子任务（各带 intent / sub_query / scope），每个子任务独立走对应 worker（kb 子查询各自检索、tool 子查询走工具），最后分点汇总。检索粒度从「整句查一次」升级为「按子问题动态并行查 N 次」，根治复合问题的主题互相稀释。
  - 提交点：`feat: 意图识别任务拆分与动态并行编排`
  - **动机（2026-09-20 真实 bug）**：问「上海明天天气 + 公司年假制度 + 餐补制度」，当前 Supervisor 只输出多标签 `kb+tool`、retrieve 仍用【整句混合 query】检索一次 → 主题互相稀释：改写漂成「年假 餐补 薪酬与职级 报销流程」，命中「概览 + 报销流程」，漏掉库里明明有的年假细则（满 1 年 5 天 / 满 5 年 10 天）与加班餐补（30 元/餐），合成只能拿国家条例兜底、且与公司实际不符。根因：检索单元是「整句」而非「原子子问题」。
  - **核心技术点**：① Supervisor 子任务化——输出从「意图 作用域」标签升级为子任务列表 `[{intent, sub_query, scope}]`（提示词 + 解析改造）；② 动态并行编排——引入 LangGraph `Send` API，route 从「返回固定节点名」升级为「按子任务数动态派发 N 个 worker 实例」（map-reduce 的 map）；③ worker 收 sub_query——retrieve / tool 从读整句改为读各自 Send 传入的子查询；④ 子结果归并（reduce）——material / sources 从单份变多份合并，**难点是 sources 跨子查询重新连续编号 `[1..M]` 且与答案 `[n]` 引用对齐**；⑤ synthesize 分点汇总——按子问题组织答案、各引用对应编号。
  - **实施前需敲定的设计决策**：① 拆分粒度（倾向「一个主题一个子查询」，年假 / 餐补分开更聚焦）；② 防过度拆分（单一问题不拆、只复合问题拆，Supervisor 要会数「含几个独立子问题」）；③ 归并编号策略（reduce 阶段统一重编 + 引用对齐）；④ 指代消解（子查询必须独立完整、带上下文补全）；⑤ 成本账（更复杂的 Supervisor 输出 + N 次检索，token / 延迟上升，用步骤9 的 trace 做子任务级成本量化）；⑥ 与现有能力的关系（防漂移护栏、Self-CRAG 有界重查在每个 kb 子查询内各自生效）。
  - **顺带优化 · 当前日期注入（上下文工程）**：把「今天是 {北京时间}」预注入 tool worker 提示词，模型无需再调 `get_current_datetime` 查日期，工具 ReAct 循环从 3 轮压到 2 轮（省一次模型调用 + 一次工具往返）。① **时间源反而更可控**：现 `get_current_datetime` 是第三方 MCP 工具、时区不受我们 `ENV TZ` 控制（黑盒），改后端注入 = 自己可控时钟 + 显式 `Asia/Shanghai` 时区；② **两个坑**：必须显式指定时区（`datetime.now(ZoneInfo("Asia/Shanghai"))`，裸 `now()` 在 UTC 容器清晨会差一天）、日期必须在 tool_node 每次执行时动态拼接（`_TOOL_SYSTEM` 是模块级常量，写死会把日期冻结在服务启动时刻）；③ **按需注入**：只给 tool worker（唯一要算相对日期的节点），supervisor / retrieve / synthesize 不需要；④ `get_current_datetime` 保留不删（模型需精确时分秒时的后备），仅提示词不再强制「先调它」。
  - **面试谈资**：query decomposition（查询分解）+ LangGraph Send 动态 map-reduce + 子结果归并与统一溯源编号 + 上下文工程（预注入当前时间压缩工具循环），是 multi-agent 编排的高级能力。
  - **步骤数变更**：新增本步，步骤总数由 13 回升为 14（原步骤 10~~13 顺延为 11~~14）。
- [x] **步骤 11 · Agent 长期记忆**：记忆官抽取 + 触发策略 + 去重更新 + 存储注入 + 治理入口。**（SQLite 存储层在本步落地——承载原步骤7 会话持久化的存储能力，且以「长期记忆」为名更具谈资。）**
  - 提交点：`feat: 跨会话长期记忆与治理`
- [x] **步骤 12 · RAG 质量保障（离线评估基线 + 在线监控面板）**：分两子阶段，先离线（12A）后在线（12B）。（依赖步骤7 的区分度语料才有得评，故排其后）
  - **12A 离线评估基线**：① 评估集生成器——LLM 读 `seed_docs` 语料自动产 QA 金标准（question / gold_answer / gold_sources），用真实用户口吻改写 + 覆盖 N7/N7 Pro/N7s 区分度陷阱防「泄题」（问题措辞贴近原文会致分数虚高），生成后人工快速抽检修正；② 检索层评估——拿 question 跑线上同款 `_retrieve_once`，对比 gold_sources 算 Recall@k / MRR / NDCG（难点：`_retrieve_once` 返回已丢 chunk_id，需用 meta 的 `(filename, section, part)` 组合对齐金标准）；③ 生成层评估——跑完整生成链路，LLM-as-judge（复用 `chat()`、零新依赖）打忠实度 / 相关性；④ 报告聚合——终端打印 + markdown/json 落地；⑤ 阈值标定——跑 Precision-Recall 曲线定 `_CRAG_SCORE_THRESHOLD` 最优值并回写 `rag.py`。
  - **12B 在线质量监控面板**：① 后端聚合端点——从 `traces` 表聚合漏标率（`cited=False` / `citation_check` 总数）、平均成本、降级率、各 purpose 的 token 分布；② 前端亮哥专属「📊 质量看板」页——复用 s5/s6 记忆治理页那套（聚合端点 + hook + 面板）。即激活步骤9 暂缓的运行看板。
  - 提交点：`feat: 离线 RAG 评估脚本`（12A）+ `feat: 在线 RAG 质量监控面板`（12B），离线/在线各一次提交
- [x] **步骤 13 · 牛来对外呈现升级（自我定位刷新）**：能力已迭代数代（多 Agent 编排、复合问题拆解并行、混合检索 + Reranker 精排、Self-CRAG、长期记忆、鉴权隔离、可观测性、离线评估），但对外呈现仍停在早期「三大能力知识库助手」，严重滞后。本步把「里子」的进化同步到所有「面子」文案，统一刷新自我定位。
  - **新定位（纲）**：牛来 = 能独当一面的企业级 AI Agent——会拆解复杂问题、多 Agent 协作、混合检索 + 精排精准找料、有长期记忆、能联网调工具，全程可观测、可评估。人设保留「沉稳可靠」底色，从「被动应答的老秘书」升级为「能替你拆解复杂活儿的老搭档」。双受众：普通用户看功能、面试官看技术深度（术语双轨）。
  - **改动范围**：① `build_persona` 自我介绍（亮哥版 + 游客版）；② `SKILL.md` 产品定位（并核对「架构开关」等描述是否过时）；③ 欢迎页 `Welcome.tsx`（副标题 + 能力卡片 + 快速问题，换成体现复合拆分 / 区分度检索 / 长期记忆的炫技问题）；④ 前端零散文案（`layout.tsx` 标题与描述 / `Sidebar` 底部 / `ChatInput` 占位符）；⑤ `mcp_server.py` 服务名片 + `build_synth_system` 能力自述；⑥ `README` 技术描述重写（清除 `dist<1.1`、单 Agent 串行、三代实现等过时内容）；⑦ `docs/images` 截图重拍。
  - **原「依赖精简」降级为可选后置**：经查证非刚需——`requirements.txt` 已是精确 freeze、`pywin32` 带 `sys_platform=="win32"` marker 可直接部署，唯一孤儿包 `langchain-classic` 无伤大雅；重建 venv 收益有限、有成本，故不阻塞发布，部署稳定后再议。
  - 提交点：`docs: 步骤13 改为对外呈现升级`；后续各文件改动分别提交
- [ ] **步骤 14 · 部署发布**：Dockerfile 适配、Reranker 模型资产核验（非 LFS 指针）、ModelScope 发布验证。
  - 提交点：`chore: 部署适配与发布验证`

## 六、编码约定

- 模块划分合理、单一职责：既不把一大堆逻辑全挤在一起，也不把逻辑拆得过散。
- 保持与现有代码库一致的详细教学式注释风格。
- 具体代码如何改，逐个模块另行讨论后再动手，不在本文档展开。

## 七、关键风险与约束

- **模型资产**：ONNX 走 LFS 时若未正确推送，会变成极小的指针文件打进镜像，需在提交后核验文件大小。
- **切片变更**：结构化切片会改变切片边界，需重新灌库。
- **依赖精简**：`requirements.txt` 中多数为间接依赖，禁止肉眼删行，必须用重建冻结法。
- **记忆触发**：Web 场景无天然「会话结束」信号，需靠惰性/轮次/闲置/强信号策略替代。
- **CPU 延迟**：精排与多 Agent 叠加会放大延迟，需控制召回量与 worker 数量。

## 八、待议项（本次改造完成后再讨论，暂不实施）

**工程增强（二档，先记录）：**

- 并发与性能：CPU 单容器的并发承载与单次问答延迟，需实测数据；当前检索为同步嵌入，注意阻塞。
- 代码测试策略：单元测试 / 集成测试（现仅有 RAG 离线评估，缺代码层测试）。
- 语义缓存：高频问题缓存结果，降延迟与成本（CPU 部署下的可选优化）。
- 限流与成本预算：防刷爆、控制 token 成本。

**面试待答的选型理由（三档，改造完再备话术）：**

- 为什么 SQLite 不用 Postgres、不上 ORM？
- 为什么 RRF 不用加权融合？
- 为什么 ONNX + int8 不用 torch？
- 为什么 Chroma 不用 Milvus / FAISS？
- 为什么 cross-encoder 放召回后、不直接全库精排？
